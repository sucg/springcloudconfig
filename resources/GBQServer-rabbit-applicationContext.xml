<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/rabbit http://www.springframework.org/schema/rabbit/spring-rabbit-1.3.xsd">
	
	<!-- 声明connection-factory -->
	<rabbit:connection-factory id="connectionFactory" 
		host="${gbq.rabbitmq.host}" port="${gbq.rabbitmq.port}"
		username="${gbq.rabbitmq.user}" password="${gbq.rabbitmq.password}" 
		virtual-host="${gbq.rabbitmq.virtualhost}"   />

	<rabbit:admin connection-factory="connectionFactory"/>

	<!-- 声明queue -->
	<bean name="myQueue" class="org.springframework.amqp.core.Queue">
		<constructor-arg value="${gbq.rabbitmq.queueName}"></constructor-arg>
		<constructor-arg value="true"></constructor-arg>
		<constructor-arg value="false"></constructor-arg>
		<constructor-arg value="false"></constructor-arg>
	</bean>
		
	<!-- exchange 和queue绑定 -->
	<rabbit:fanout-exchange id="myExchange" name="${gbq.rabbitmq.exchangeName}" auto-delete="false" durable="true" >
		<rabbit:bindings>
			<rabbit:binding queue="myQueue" />
		</rabbit:bindings>
	</rabbit:fanout-exchange>

	<!-- 声明消费者 listener形式 -->
	<rabbit:listener-container 
		connection-factory="connectionFactory" acknowledge="manual"
		message-converter="jsonMessageConverter">    <!-- 手动确认消息 -->
		<rabbit:listener ref="gbqListener" method="onMessage" queues="myQueue" />
	</rabbit:listener-container>

	<bean id="gbqListener" class="com.glodon.gbq.message.util.RabbitMqListener"></bean>

	<bean id="jsonMessageConverter"
		class="org.springframework.amqp.support.converter.Jackson2JsonMessageConverter"></bean>
	
</beans>	